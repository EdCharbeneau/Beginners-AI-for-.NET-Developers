# Working with text, streaming and image content types

This folder contains the code for the "Microsoft.Extensions.AI Basic Concepts" module. The module is designed to help you understand how to work with text, streaming and image content types using Microsoft Extensions AI.

Each folder corresponds to the video sections in the module, and contains the code for the respective section. The structure of the folders is as follows. The "Start" folder contains the code before the changes are made in the video, and the "End" folder contains the code after the changes are made. You can use these folders to follow along with the video and see how the code evolves throughout the module.

Each section folder includes a .slnx solution file that references both the Start and End projects, making it easy to open and work with both versions in Visual Studio.

```
08 - Working with text, streaming and image content types
├── 8.1 - Blazor Project Intro
│   └── Blazor Project Intro.slnx
├── 8.2 - IChatClient Dependencies and Configuration
│   ├── 8.2 - IChatClient Dependencies and Configuration.slnx
│   ├── IChatClient Dependencies and Configuration - Start
│   └── IChatClient Dependencies and Configuration - End
├── 8.3 - Chat UI setup
│   ├── 8.3 - Chat UI setup.slnx
│   ├── Chat UI setup - Start
│   └── Chat UI setup - End
├── 8.4 - AIContent and content types
│   ├── 8.4 - AIContent and content types.slnx
│   ├── AIContent and content types - Start
│   └── AIContent and content types - End
├── 8.5 - Using image content with multimodal model input
│   ├── 8.5 - Using image content with multimodal model input.slnx
│   ├── Using image content - Start
│   └── Using image content - End
├── 8.6 - How to Enable Logging on IChatClient
│   ├── 8.6 - How to Enable Logging on IChatClient.slnx
│   ├── How to enable logging - Start
│   └── How to enable logging - End
└── 8.7 - How to use GetStreamingResponseAsync to stream text from IChatClient
	├── 8.7 - How to use GetStreamingResponseAsync to stream text from IChatClient.slnx
	├── How to use GetStreamingResponseAsync - Start
	└── How to use GetStreamingResponseAsync - End
```