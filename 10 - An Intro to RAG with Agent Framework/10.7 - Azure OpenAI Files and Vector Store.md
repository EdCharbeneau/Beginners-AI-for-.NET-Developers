# Azure OpenAI Files and Vector Store

The video for this step has no coding exercises. However, it shows where to find the files and vector storage within Foundry.

At the present time, the new Foundry UI does not display the files and vectors properly. The old version must be used to access the feature.